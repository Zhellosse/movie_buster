{% extends 'admin.html.twig' %}

{% block title %}Actors{% endblock %}

{% block body %}
    <h1>Acteur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ actor.id }}</td>
            </tr>
            <tr>
                <th>Prénom</th>
                <td>{{ actor.firstName }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ actor.lastName }}</td>
            </tr>
            <tr>
                <th>Date de naissance</th>
                <td>{{ actor.birthDate ? actor.birthDate|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('actors_index') }}">Retour a la liste des acteurs</a>

    <a href="{{ path('actors_edit', {'id': actor.id}) }}">Modifier</a>

    {{ include('actors/_delete_form.html.twig') }}
{% endblock %}
