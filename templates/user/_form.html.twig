{{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_row(form.password) }}
    {{ form_row(form.avatar) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
